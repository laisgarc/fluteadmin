[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Flute Admin"
authors = [{name = "Flute", email = "laisgarcia@vorttex.com.br"}]
dynamic = ["version", "description"]
